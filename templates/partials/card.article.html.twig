            {% for item in article %}
                
        <div class="col-4">
            <div class="card bg-light my-5 shadow">
                <h3 class="card-header">{{item[0] | title}}</h3>
                <img height="350px"  src="{{ item[3] }}" alt="Card image">
                <div class="card-body">
                <p class="card-text">{{item[1]}}</p>
                <a href="#" class="btn btn-primary rounded">Lire la suite</a>
                <div class="my-3">
                    <a href="#" class="card-link">{{item[2]}}</a>
                </div>
                </div>
                <div class="card-footer text-muted">
                Il y a 2 jours
                </div>
            </div>
        </div>
            {% endfor %}